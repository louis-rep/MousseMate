[build]
builder = "nixpacks"
# Railway build machines have plenty of RAM; a 512MB cap makes V8 abort while bundling plotly
buildCommand = "npm install && NODE_OPTIONS=--max-old-space-size=4096 npm run build"

[deploy]
startCommand = "npm run preview -- --host --port $PORT"
